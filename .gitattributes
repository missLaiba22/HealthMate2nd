# Normalize all text files to LF line endings
* text=auto

# Treat Dart, Python, and config files as text
*.dart text
*.py text
*.json text
*.yaml text
*.yml text

# Ignore diffs on binary files (images, icons)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary

# Handle Windows line endings correctly for scripts
*.sh text eol=lf
*.bat text eol=crlf
